#include "FauxMatStack.hpp"

namespace LE {

  void FauxMatStack::push(const Translation t) {
	//translationStack.push_back(t);
  }

  void FauxMatStack::push(const float x, const float y) {
	//translationStack.push_back(Translation(x, y));
  }

  void pop() {
	//translationStack.pop_back();
  }

} // end namespace LE
